{
  "name": "gate-quiz-app",
  "version": "1.0.0",
  "description": "GATE Exam Preparation Quiz Application",
  "main": "index.js",
  "scripts": {
    "dev": "set PORT=3001 && react-scripts start",
    "start": "set PORT=3001 && react-scripts start",
    "build": "set BUILD_PATH=../build && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "react-router-dom": "^6.8.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "proxy": "http://localhost:5000"
}
